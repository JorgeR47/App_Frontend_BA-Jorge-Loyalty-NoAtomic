import React from 'react';
import { ActivityIndicator } from 'react-native';

const AtomLoadingIndicator: React.FC = () => (
  <ActivityIndicator size="large" color="#4caf50" />
);

export default AtomLoadingIndicator;
